# JobIntel

WSL Ubuntu + Python project to analyze job descriptions and extract signals (e.g., clearance/citizenship requirements).

## Dev setup (WSL)
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
